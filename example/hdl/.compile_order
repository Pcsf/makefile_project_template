# VHDL compilation order for: example/hdl/
# Generated by scan_project.sh — edit freely, never overwritten by rescan.
#
# One filename per line (no directory path), in compilation order.
# Rule: a package or entity must appear BEFORE any file that uses it.
#   e.g. if top.vhd instantiates counter, list counter.vhd first.
# Lines starting with # and blank lines are ignored.
#
# NOTE: inter-directory ordering (hdl/modules/ compiled before hdl/) is
# handled by the order in which the root Makefile includes Makefile.mk files.
# find(1) returns deeper directories first, so hdl/modules/ is added to
# VHDL_SRCS before hdl/ — which is the correct order here.
# If your directory layout requires a different cross-directory order,
# use the VHDL_SRCS global override in project.mk.
#
top.vhd
